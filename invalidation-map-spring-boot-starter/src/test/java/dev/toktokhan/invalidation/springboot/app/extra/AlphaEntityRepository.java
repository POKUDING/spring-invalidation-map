package dev.toktokhan.invalidation.springboot.app.extra;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AlphaEntityRepository extends JpaRepository<AlphaEntity, Long> {
}
