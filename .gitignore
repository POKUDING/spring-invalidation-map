.gradle/
build/
*.class
.idea/
*.iml
.DS_Store

# 이 저장소는 라이브러리 코드만 담습니다. 설계 문서와 구현 계획은 진행 과정의
# 산출물이고 참조하는 실제 프로젝트의 내부 구조(리포지토리·엔티티 이름, 엔드포인트
# 경로)를 담고 있어 배포 대상이 아닙니다. 로컬에는 그대로 남습니다.
docs/

# SDD 작업공간(리뷰·재리뷰 기록, 진행 원장)입니다.
.superpowers/
